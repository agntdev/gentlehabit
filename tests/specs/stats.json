[
  { "name": "/stats has a clear empty state", "steps": [
    { "send": { "text": "/stats" }, "expect": [{ "method": "sendMessage", "payload": { "text": "No habits yet — tap ➕ Create habit to begin." } }] }
  ] }
]
